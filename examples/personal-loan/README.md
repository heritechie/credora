# Personal Loan Example

This directory will contain the first example assessment configuration for
Credora.

It is intentionally empty for now. During the engine implementation phase it
will hold a runnable example that wires real (or mock) providers, a
deterministic policy, scoring, evidence, and an audit trail into a complete
personal-loan assessment the way a developer would configure it.